# virtual-audio-device-simulator
for nerds: a way to simulate a basic audio device which supports real time write() and read() operations with thread safety and a producer/listener relationship with C++
